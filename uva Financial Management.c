#include<stdio.h>
int main()
{
    int t,i,tc=0,a;
    float ttl=0,avg;
    scanf("%d",&t);
    while(t--)
    {
        ttl=0;
        for(i=0; i<12; i++)
        {
            scanf("%d",&a);
            ttl+=a;
        }
        avg=ttl/12.0;
        if(ag)
        printf("%d $.2%f",++tc,avg);
    }
}

